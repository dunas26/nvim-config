# Neovim Personal Configuration

Custom commands and plugins for neovim

# Setup

In order to have a properly working config you need to fill these prerequisites

- Install wbthomason/packer.nvim. [See how](https://github.com/wbthomason/packer.nvim#quickstart)
- Inside neovim, execute `:PackerInstall` to install all the plugin packages
